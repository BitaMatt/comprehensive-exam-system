# 更新日志

## v1.3.1 - 2026-06-01

- 题库页新增固定操作菜单，题库展开后仍可直接执行 AI 补解析、编辑或删除。
- 新增题库 JSON 导入功能，导入文件会保存到本机应用数据目录，不写入 Git 目录。
- 新增题库模板 `templates/question_bank_template.json`，可交给 GPT 按模板生成后导入。
- 新增 AI 生成答案解析流程，支持导入后、PDF 生成后勾选自动补解析，也可对已有题库手动补解析。
- 新增简体中文/繁体中文显示切换，题库内容在界面显示时会做简繁转换。

## v1.3.0 - 2026-05-26

- 新增题库生成功能，可在应用内选择 PDF 并自动生成可练习题库。
- PDF 处理采用混合策略：优先本地抽取文本，疑似扫描页渲染为图片后交给兼容 OpenAI 的视觉/OCR 模型处理。
- 当 API Key 不支持图片分析时，Windows 端新增本地 OCR 降级路径，可用 RapidOCR/Tesseract 先转文字再交给文本模型整理。
- 生成题库新增调试日志，显示 PDF 渲染、OCR、AI 解析和降级重试的实际进度。
- 题库页支持编辑或删除用户生成的题库，内置题库保持只读。
- AI 解析按页组分片执行，降低大 PDF 超时和 token 过大的风险。
- 生成过程支持可视化进度、暂停、失败提示和从上次任务继续。
- 生成题库保存到本机应用数据目录，启动后自动加载到题库列表，并支持导出 JSON。
- 新增题库校验和生成任务续跑相关测试。

## v1.2.1 - 2026-05-23

- 统一将 Windows 安装包、Windows 便携包和 Android APK 输出到项目内的 `out/` 目录。
- 新增应用内 AI 设置窗口：首次启动强制配置，之后可从顶部 AI 设置入口随时修改。
- AI 设置支持 Base URL、API Key、模型名配置，默认兼容 ChatAnywhere 的 OpenAI 风格 `/v1` 接口。
- 新增测试连接和获取模型列表功能。
- 新增 VS Code 调试与打包配置，以及 `scripts/package_out.ps1` 一键打包脚本。

## v1.2.0 - 2026-05-22

- 使用 Flutter 重构应用，支持 Windows 和 Android 双端运行。
- 新增跨端练习界面、题库浏览、练习记录和 Windows/Android 打包流程。
